apiVersion: v1
kind: Service
metadata:
  name: vendor-service
  namespace: {{ .Values.namespace }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: vendor-service
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
---
apiVersion: v1
kind: Service
metadata:
  name: vendor-service-preview
  namespace: {{ .Values.namespace }}
spec:
  type: ClusterIP
  selector:
    app: vendor-service
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}


